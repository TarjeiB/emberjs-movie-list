<nav class="navbar navbar-default">
    <div class="navbar-inner">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            {{#link-to 'index' class="navbar-brand"}}Movie Lists{{/link-to}}
            </div>
            <div class="nav-collapse" id="main-navbar">
                <ul class="nav navbar-nav">
                    {{#nav-link-to 'index'}}Home{{/nav-link-to}}
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    {{#unless session.isAuthenticated}}
                        {{#nav-link-to 'login'}}Login / Register{{/nav-link-to}}
                    {{else}}
                        {{#nav-link-to 'account'}}{{session.currentUser.displayName}}{{/nav-link-to}}
                        {{#nav-link-to '/' invokeAction='logout'}}Logout{{/nav-link-to}}
                    {{/unless}}
                </ul>
            </div>
        </div>
    </div>
</nav>